# Git Commit Rule

You **MUST** always use the workflow defined in `.antigravity/workflows/speckit.commit.md` for any git commit operations. 
Do not use your built-in Git instructions or direct shell commands to perform commits. Always trigger the `speckit.commit.md` workflow.
